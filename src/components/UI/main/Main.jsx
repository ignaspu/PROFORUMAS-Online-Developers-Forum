import styled from "styled-components";
import Filter from "../filter/Filter";

const StyledMain = styled.main`

`;

const Main = () => {
  return ( 
    <StyledMain>
      <Filter/>
    </StyledMain>
   );
}
 
export default Main;